# Copy to .env and edit for local development
APP_SECRET=change-me-to-a-random-secret
LOGIN_USER=admin
LOGIN_PASS=changeme
# Or set LOGIN_PASS_HASH=<werkzeug-generated-hash> to avoid storing plain password
